<title>RADᗡAR</title>
<div class="page-header"><h1>THIS IS RADAR</div>
<small>This are our main stories for today</small><h1>
<% @posts.each do |post| %>

<h4><%= post.subject %></h4>
<h5><%= post.topic %>
    <h5>
        <h6>Location
            <%= post.place %>
            | Date
            <%= post.date %></h6>
        <div class="form-group">
            <label for="comment"></label>
            <textarea class="form-control" rows="5" id="comment"><%= post.data[0...200] %></textarea>
        </div>
        Comments

        <%= post.comments.count  %>

        <%= link_to "Details", post_path(post) %>
        <% end %>
